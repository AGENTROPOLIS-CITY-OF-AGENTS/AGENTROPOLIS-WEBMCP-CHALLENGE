# Quarantine

Use this directory for unreviewed upstream imports, screenshots, notes, and temporary migration artifacts.

Rules:

- Do not export directly from `quarantine/`.
- Do not move code into `src/` until provenance and license information are recorded.
- Every promotion from `quarantine/` must update the manifest maturity and `provenance/SOURCELOG.md`.
